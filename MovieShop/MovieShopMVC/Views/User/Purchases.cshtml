@model IEnumerable<ApplicationCore.Models.MovieCardResponseModel>

@{
    ViewData["Title"] = "Purchases";
}

<div class="rounded">
    <div class="container-fluid">
        <div class="row">
            @foreach (var movie in Model)
            {
                <div class="col-xl-2 col-sm-4 col-lg-3">
                    @*call partial view from here and send the movie info object to partial view*@
                    <partial name="_MovieCard" model="movie" />
                </div>
            }
        </div>
        <div id="Modal">

        </div>
        <button id="button" class="btn btn-link">Next Page</button>
        <script>

            document.querySelector("#button").addEventListener("click", async function () {
              
                
           

            })
        </script>
    </div>
</div>